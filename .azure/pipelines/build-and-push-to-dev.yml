trigger:
  branches:
    include:
      - azure-pipelines

pr: none

pool:
  vmImage: ubuntu-latest

variables:
  - group: chess-stats-dev-variable-group

extends:
  template: ./templates/build-and-push-to-registry.yml
  parameters:
      AppInsightsKey: $(APP-INSIGHTS-KEY)
      BranchName: azure-pipelines
      Environment: chess-stats-dev
